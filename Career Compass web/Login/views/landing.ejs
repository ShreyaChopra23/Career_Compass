<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CareerCompass - Your Career Guide</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(120deg, #f6f8fd 0%, #f1f4f9 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem;
        }
        .auth-container {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 30px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.08);
            overflow: hidden;
            width: 100%;
            max-width: 1000px;
            border: 1px solid rgba(255, 255, 255, 0.8);
        }
        .header {
            text-align: center;
            padding: 3rem 2rem;
            background: linear-gradient(135deg, #3a7bd5 0%, #00d2ff 100%);
            color: white;
            position: relative;
            overflow: hidden;
        }
        .header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0) 100%);
            z-index: 1;
        }
        .header h1, .header p {
            position: relative;
            z-index: 2;
        }
        .boxes-container {
            display: flex;
            flex-wrap: wrap;
            gap: 2.5rem;
            padding: 3rem 2rem;
            justify-content: center;
            background: linear-gradient(180deg, #ffffff 0%, #f8faff 100%);
        }
        .auth-box {
            flex: 0 1 400px;
            padding: 2.5rem;
            border-radius: 20px;
            background: white;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.04);
            text-decoration: none;
            color: inherit;
            transition: all 0.4s ease;
            text-align: center;
            border: 1px solid #f0f0f0;
            position: relative;
            overflow: hidden;
        }
        .auth-box::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #3a7bd5, #00d2ff);
            transform: scaleX(0);
            transition: transform 0.4s ease;
        }
        .auth-box:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(58, 123, 213, 0.1);
        }
        .auth-box:hover::before {
            transform: scaleX(1);
        }
        .auth-box img {
            width: 180px;
            height: 180px;
            object-fit: contain;
            margin-bottom: 2rem;
            transition: transform 0.4s ease;
        }
        .auth-box:hover img {
            transform: scale(1.05);
        }
        .auth-box h3 {
            color: #2d3748;
            margin-bottom: 1rem;
            font-size: 1.5rem;
            font-weight: 600;
        }
        .auth-box p {
            color: #718096;
            margin-bottom: 1.8rem;
            line-height: 1.6;
        }
        .btn-get-started {
            display: inline-block;
            padding: 14px 32px;
            background: linear-gradient(135deg, #3a7bd5 0%, #00d2ff 100%);
            color: white;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(58, 123, 213, 0.2);
        }
        .btn-get-started:hover {
            opacity: 0.95;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(58, 123, 213, 0.3);
        }
        .welcome-text {
            font-size: 1.2rem;
            opacity: 0.95;
            margin-top: 1rem;
            font-weight: 300;
        }
    </style>
</head>
<body>
    <div class="auth-container">
        <div class="header">
            <h1 class="mb-3">Welcome to CareerCompass</h1>
            <p class="welcome-text">Your intelligent companion for career guidance and professional development</p>
        </div>
        <div class="boxes-container">
            <!-- Login Box -->
            <a href="/login-form" class="auth-box">
                <img src="https://img.freepik.com/free-vector/login-concept-illustration_114360-739.jpg" alt="Login Illustration">
                <h3>Login</h3>
                <p>Already have an account? Sign in to continue your career journey.</p>
                <div class="btn-get-started">Login Now</div>
            </a>

            <!-- Sign Up Box -->
            <a href="/signup-form" class="auth-box">
                <img src="https://img.freepik.com/free-vector/sign-up-concept-illustration_114360-7885.jpg" alt="Sign Up Illustration">
                <h3>Sign Up</h3>
                <p>New to CareerCompass? Create an account to start your career exploration.</p>
                <div class="btn-get-started">Sign Up Now</div>
            </a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 