<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - CareerCompass</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(120deg, #f6f8fd 0%, #f1f4f9 100%);
            min-height: 100vh;
            padding: 2rem;
        }
        .dashboard-container {
            max-width: 1200px;
            margin: 0 auto;
        }
        .header {
            text-align: center;
            padding: 2rem;
            background: linear-gradient(135deg, #3a7bd5 0%, #00d2ff 100%);
            color: white;
            border-radius: 20px;
            margin-bottom: 2rem;
            position: relative;
            overflow: hidden;
        }
        .header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0) 100%);
            z-index: 1;
        }
        .header h1, .header p {
            position: relative;
            z-index: 2;
        }
        .welcome-text {
            font-size: 1.2rem;
            opacity: 0.9;
        }
        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            padding: 1rem;
        }
        .feature-box {
            background: white;
            border-radius: 20px;
            padding: 2rem;
            text-align: center;
            transition: all 0.3s ease;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.8);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
        }
        .feature-box::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #3a7bd5, #00d2ff);
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }
        .feature-box:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(58, 123, 213, 0.1);
        }
        .feature-box:hover::before {
            transform: scaleX(1);
        }
        .feature-icon {
            font-size: 3.5rem;
            margin-bottom: 1.5rem;
            background: linear-gradient(135deg, #3a7bd5, #00d2ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .feature-box h3 {
            color: #2d3748;
            margin-bottom: 1rem;
            font-size: 1.5rem;
        }
        .feature-box p {
            color: #718096;
            margin-bottom: 1.5rem;
            line-height: 1.6;
        }
        .nav-links {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 2rem;
            background: white;
            border-radius: 15px;
            margin-bottom: 2rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        }
        .logout-btn {
            background: linear-gradient(135deg, #3a7bd5 0%, #00d2ff 100%);
            color: white;
            padding: 0.5rem 1.5rem;
            border-radius: 8px;
            text-decoration: none;
            transition: all 0.3s ease;
        }
        .logout-btn:hover {
            opacity: 0.9;
            color: white;
        }
        .user-name {
            color: #2d3748;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <nav class="nav-links">
            <span class="user-name">Welcome, <%= name %> 👋</span>
            <a href="/logout" class="logout-btn">Logout</a>
        </nav>
        
        <header class="header">
            <h1 class="mb-3">Your Career Dashboard</h1>
            <p class="welcome-text">Explore tools and resources to guide your career journey</p>
        </header>

        <div class="features-grid">
            <!-- Profile Box -->
            <a href="/profile" class="feature-box text-decoration-none">
                <div class="feature-icon">
                    <i class="fas fa-user-circle"></i>
                </div>
                <h3>Profile</h3>
                <p>View and edit your profile information, update preferences, and manage your account settings.</p>
            </a>

            <!-- Career Path Box -->
            <a href="/career-path" class="feature-box text-decoration-none">
                <div class="feature-icon">
                    <i class="fas fa-road"></i>
                </div>
                <h3>Career Path</h3>
                <p>Explore different career paths, get personalized recommendations, and plan your journey.</p>
            </a>

            <!-- AI Chatbot Box -->
            <a href="/chat" class="feature-box text-decoration-none">
                <div class="feature-icon">
                    <i class="fas fa-robot"></i>
                </div>
                <h3>AI Chatbot</h3>
                <p>Get instant answers to your career questions and receive guidance from our AI assistant.</p>
            </a>

            <!-- Skill Gap Analysis Box -->
            <a href="/skill-gap" class="feature-box text-decoration-none">
                <div class="feature-icon">
                    <i class="fas fa-chart-bar"></i>
                </div>
                <h3>Skill Gap Analysis</h3>
                <p>Identify areas for improvement and get personalized recommendations for skill development.</p>
            </a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>